[build]
  command = "cd frontend && npm ci --include=dev && npm run build"
  publish = "frontend/build"
  node_version = "24.x"

# React client-side routing - all routes go to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[dev]
  command = "npm run dev"
  port = 3000

[context.production]
  environment = { NODE_ENV = "production" }

[context.deploy-preview]
  environment = { NODE_ENV = "production" }
